export enum Role {
  DRIVER = 'ROLE_DRIVER'
}

export enum DriverStatus {
  ACTIVE = 'ACTIVE',
  SUSPENDED = 'SUSPENDED'
}

export enum DriverState {
  AVAILABLE = 'AVAILABLE',
  BUSY = 'BUSY',
  ON_TRIP = 'ON_TRIP'
}

export enum OnlineStatus {
  ONLINE = 'ONLINE',
  OFFLINE = 'OFFLINE'
}
